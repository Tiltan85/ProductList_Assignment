<UserControl x:Class="Presentation.WPF.Views.ProductAddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WPF.Views"
             
             xmlns:models="clr-namespace:Infrastructure.Models;assembly=Infrastructure"
             xmlns:viewmodels="clr-namespace:Presentation.WPF.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:ProductAddViewModel}"
             
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="500" d:DesignWidth="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
               Text="{Binding Title, FallbackValue= New Product}" 
               FontSize="20" 
               FontWeight="Bold" 
               VerticalAlignment="Center"/>

        <StackPanel Grid.Row="1">
            <Label Content="Product Name" />
            <TextBox Text="{Binding ProductRequestForm.ProductName}" Padding="5" Margin="0 0 0 5" />

            <Label Content="Product Price (SEK)" />
            <TextBox Text="{Binding ProductRequestForm.ProductPrice}" Padding="5" Margin="0 0 0 5" />

            <Label Content="Product Description" />
            <TextBox Text="{Binding ProductRequestForm.ProductDescription}" Padding="5" Margin="0 0 0 5" />

            <Label Content="Product Category" />
            <TextBox Text="{Binding ProductRequestForm.Category}" Padding="5" Margin="0 0 0 5" />

            <Label Content="Product Manufacturer" />
            <TextBox Text="{Binding ProductRequestForm.Manufacturer}" Padding="5" Margin="0 0 0 5" />
        </StackPanel>

        <StackPanel Grid.Row="2">
            <Button Content="Save" Command="{Binding SaveCommand}" Padding="8" Margin="0 0 0 8" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" Padding="8" />
            
        </StackPanel>

    </Grid>
</UserControl>
